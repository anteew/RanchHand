{
  "name": "ranchhand-mcp",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "RanchHand: OpenAI-compatible MCP server for Ollama and compatible backends.",
  "scripts": {
    "start": "node server.mjs",
    "cli": "node cli.mjs",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0"
  },
  "devDependencies": {
    "eslint": "^9.37.0"
  }
}
